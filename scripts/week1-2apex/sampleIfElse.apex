string s1='Merhaba';
if (s1== ''){
system.debug('S1 is blank OKAY?');
}else{
    system.debug(s1);
}

if(string.isblank(s1)){
    system.debug('S1 is still blank, okayy?');
}else{
    system.debug(s1);
}